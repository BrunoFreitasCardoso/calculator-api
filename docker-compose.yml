services:
  rest:
    build:
      context: ./rest
      dockerfile: Dockerfile
    ports:
      - "8080:8080"

  calculator:
    build:
      context: ./calculator
      dockerfile: Dockerfile
    ports:
      - "8081:8081"

networks:
  my-network:
    driver: bridge
